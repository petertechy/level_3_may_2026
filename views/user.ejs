<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>List of Users</h1>

    <!-- <% for( let index = 0; index < students.length; index++ ) { %>
        <p><%= index+1 %>.</p>
        <h1><%= students[index].firstname %> <%= students[index].lastname %></h1>
        <p><%= students[index].email %></p>
        <i><%= students[index].gender %></i>
    <% } %> -->

    <% students.forEach(users => { %>
        <h1><%= users.firstname %> <%= users.lastname %></h1>
        <p><%= users.email %></p>
        <i><%= users.gender %></i>
    <% }) %>
</body>
</html>